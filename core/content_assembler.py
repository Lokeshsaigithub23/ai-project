from core.platform_formatter import build_platform_content

def assemble_content(article, caption, hashtags):

    return build_platform_content(
        article,
        caption,
        hashtags
    )